/**
 * Ambient declarations for vendor stylesheet imports that are subpath exports
 * without a `.css` suffix (so the built-in `*.css` ambient module won't match).
 */
declare module "@waline/client/style";
