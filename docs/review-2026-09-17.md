# Blog 项目审查与 Search Console 排查

日期：2026-09-17。范围：源码、构建产物和 Google Search Console 当前报告；未取得 Core Web Vitals 现场数据，不把潜在性能问题表述为已发生的指标不达标。

## 结论与验证状态

- 项目使用 Astro 静态构建，评论和乐谱组件已采用按需加载，现阶段无需重写架构。
- 修改前基线：`pnpm build`、`pnpm lint`、`pnpm format:check` 均通过。
- 本轮已在本地修复：从 sitemap 排除搜索页、消除重复 `og:type`、将首页关于链接改为 `/about/`。未改动已有 canonical 生成逻辑。
- 修改后 `pnpm build`（含 Astro 检查）、`pnpm lint`、`pnpm format:check`、`git diff --check` 均通过。Astro 检查为 0 errors / 0 warnings / 0 hints；Vite 仍提示既有大 JS chunk，可参考下方媒体加载优化。独立代码复核通过。
- 构建产物复验：sitemap 不再包含 `/search/`，4 篇文章的重复 `og:type` 已消除，首页关于链接为 `/about/`。本报告记录发布前的审查结果；发布状态以对应 GitHub Actions 运行和线上复验为准。

## Search Console

当前报告显示 **12 个已编入索引、19 个未编入索引**。未收录原因需要分别处理，不能将 19 个页面都视为网站故障。

报告最后更新于 2026-09-14，本次读取于 2026-09-17。已收录示例中确认：首页、`/about/`、全部 4 篇现有文章、`/posts/`、`/tags/`、`/archives/`、Git/Linux 标签页及评论服务首页。AE-20 网址检查也明确显示“网址已收录到 Google”；它的视频索引状态与普通网页索引状态不同。

| 报告项 | 已确认的情况 | 处理结论 |
| --- | --- | --- |
| 11 个 404 | 5 个旧文章地址，以及 JavaScript、CSS、代理、博客、Angular 分页等旧标签地址；来自 7 月 6 日主动删除 26 篇旧文的提交 `24bddb93` | 删除是预期行为；没有对应替代内容时保留 404，不应全部重定向至首页。GSC 已有 9 月 17 日启动的验证，本次未重复启动 |
| `/search/` 被 noindex 排除 | 搜索结果页不收录正常，但该页仍被列入 sitemap | 保留 noindex，从 sitemap 移除；本轮修复 |
| HTTP → HTTPS 重定向 | 网站协议规范化产生的重定向 | 正常，提交并使用 HTTPS 规范地址 |
| 其余 6 个地址 | `/tags/wsl/`、`/about`、2 个评论服务 RSS 地址、`/tags/aerophone-ae-20/`、`/tags/react-native/` | `/about` 重定向至 `/about/`，应使用后者作为规范地址；标签页需结合内容价值和后续抓取判断；评论 RSS 单独处理 |
| Sitemap | 提交成功，发现 15 个网址；最后读取时间为 9 月 13 日 | 无 sitemap 抓取失败证据；发布修正版本后等待重新读取 |
| AE-20 视频 | 页面混合开箱视频、另一段视频和音乐，Google 将其判定为非视频观看页 | 是否拆成以单个视频为主的独立观看页，待用户选择；当前保留原文章结构，视频索引问题尚未修复 |

评论 API / RSS 位于独立的 `comments.lvjianqiao.com` 服务。博客中的 `src/utils/waline.ts` 仅配置服务地址，不能假定修改博客 robots 或页面配置即可控制该服务的索引行为。

线上抽查确认：robots、sitemap、搜索页和 AE-20 文章返回 200；`/about` 跳到 `/about/`，canonical 一致；已删除的 Nuxt 文章返回真实 404。没有证据表明现有文章被 robots、noindex 或错误 canonical 阻止收录。三个未收录标签页分别只有对应文章列表，Google 可自行决定是否收录，不能承诺修改后立即收录。

处理依据：[Google 网页索引说明](https://support.google.com/webmasters/answer/7440203)明确允许已删除且无替代内容的页面返回 404，也说明“已抓取但未编入索引”不需要重复提交；[Google 视频索引说明](https://support.google.com/webmasters/answer/9495631)要求观看页以单个视频为主要内容；sitemap 排除方式依据 [Astro 官方 filter 文档](https://docs.astro.build/en/guides/integrations-guide/sitemap/#filter)。

## 可优化项

优先级 P2 表示值得安排修复，P3 表示后续优化；以下尚未全部纳入本轮代码修改。

| 优先级 | 问题与证据 | 建议 |
| --- | --- | --- |
| P2 | `src/components/WalineStats.astro:66–90` 在页面切换时取消了请求，却未取消尚未执行的 idle/timeout。对原脚本做 Node VM 调度模拟，A → B 后两个回调均给 B 发起阅读量统计 | 保存并取消延迟任务，回调绑定当前页面路径，防止过期任务计数 |
| P2 | `.github/workflows/deploy-oss.yml:84–93` 将整个 dist 并发上传，不能保证注释所说的资源先于 HTML 上线；取消中的部署也可能留下不完整发布 | 将静态资源上传和 HTML 发布拆成顺序阶段 |
| P2 | 当前构建产物中，3 篇图文共 16 张图片全部没有 width/height；`src/utils/rehypeImgAttrs.ts:10–14` 仅补充 lazy/async | 补充图片尺寸或宽高比，减少图片加载时的布局跳动；首屏图片单独选择加载策略 |
| P2 | `src/pages/posts/[...slug]/index.astro:175–206,539–541` 的内联脚本随文章导航重复注册 document 监听。对原脚本模拟执行 3 次，得到 3 个 scroll 和 3 个 after-swap 监听 | 注册一次或在页面切换前清理监听 |
| P3 | `MediaPlayer.astro:113` 和 `AudioPlayer.astro:44` 在页面加载时引入播放器；本次构建约 114 KB + 59 KB JS，gzip 约 34 KB + 15 KB | 靠近视口再加载播放器，并显式设置媒体 preload 策略 |
| P3 | `package.json:10` 将 Pagefind 产物反复复制回 public。本地存在 9 份 metadata、154 个 fragment，但当前仅索引 4 篇文章 | 隔离开发索引缓存并清理过期文件，避免本地构建目录累积 |

上述模拟验证用于确认监听和异步调度问题，不等同于真实浏览器性能测试。布局跳动、媒体加载收益需要在修复后通过浏览器测量确认。
